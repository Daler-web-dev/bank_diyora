{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite\" \"json-server -p 7777 -w db.json\"",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"Eror:no test specified\"&& exit 1",
    "style": "node-sass -w ./scss -0 ./css"
  },
  "devDependencies": {
    "vite": "^4.2.1"
  },
  "dependencies": {
    "axios": "^1.3.5",
    "sass": "^1.60.0"
  }
}
